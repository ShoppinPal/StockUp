import { OrgStoresPipe } from './org-stores.pipe';

describe('OrgStoresPipe', () => {
  it('create an instance', () => {
    const pipe = new OrgStoresPipe();
    expect(pipe).toBeTruthy();
  });
});
