import { SupplierStoresPipe } from './supplier-stores.pipe';

describe('SupplierStoresPipe', () => {
  it('create an instance', () => {
    const pipe = new SupplierStoresPipe();
    expect(pipe).toBeTruthy();
  });
});
