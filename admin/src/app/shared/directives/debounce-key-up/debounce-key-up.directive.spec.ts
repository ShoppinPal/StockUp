import { DebounceKeyUpDirective } from './debounce-key-up.directive';

describe('DebounceKeyUpDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceKeyUpDirective();
    expect(directive).toBeTruthy();
  });
});
